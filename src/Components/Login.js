import React from 'react';
import '../Css/navbar.css';

const Login = () => {
  return (
    <button type="button" onClick={console.log('test Login button')}>
      Login
    </button>
  );
};

export default Login;
